# KPI Digital - Environment Variables Configuration

# ==================================
# REQUIRED FOR PRODUCTION
# ==================================

# Resend API Key (Required for email functionality)
# Get your API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxx

# Email From Address (Required for Resend)
# Must be a verified domain or use onboarding@resend.dev for testing
# Example: noreply@kpidigital.com.au
EMAIL_FROM=noreply@kpidigital.com.au

# Session Secret (Required for secure sessions)
# Generate a secure random string (at least 32 characters)
# Example: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-secure-random-secret-here

# ==================================
# OPTIONAL / PRODUCTION SETTINGS
# ==================================

# Allowed Origins for CORS (comma-separated, defaults to https://kpidigital.com.au)
# Include all production domains: apex, www, staging
# Example: ALLOWED_ORIGINS=https://kpidigital.com.au,https://www.kpidigital.com.au
ALLOWED_ORIGINS=https://kpidigital.com.au

# Node Environment (defaults to 'development')
NODE_ENV=production

# Server Port (defaults to 5000)
PORT=5000

# ==================================
# AUTO-CONFIGURED (No Action Needed)
# ==================================

# The following are automatically configured by Replit:
# - DATABASE_URL (PostgreSQL connection string)
# - DEFAULT_OBJECT_STORAGE_BUCKET_ID
# - PRIVATE_OBJECT_DIR
# - PUBLIC_OBJECT_SEARCH_PATHS
